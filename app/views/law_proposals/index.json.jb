# frozen_string_literal: true

@law_proposals.map do |law_proposal|
  {
    id: law_proposal.id,
    name: law_proposal.name,
    description: law_proposal.description,
    url: law_proposal_url(law_proposal, format: :json)
  }
end
