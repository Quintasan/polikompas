FROM busybox
ARG UID=1000
ARG GID=1000
ENV USER_ID=$UID
ENV GROUP_ID=$GID
RUN addgroup -g ${GROUP_ID} -S app && adduser -u ${USER_ID} -S app -G app
RUN mkdir /node_modules && chown -R app:app /node_modules
